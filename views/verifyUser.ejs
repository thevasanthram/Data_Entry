<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- <link rel="stylesheet" href="./verifyUser.css" /> -->
    <title>Data Entry System</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <!-- !...............User Activation Section......... -->
    <div style="height: 100vh;" class="h-full bg-gradient-to-tl from-green-400 to-indigo-900 w-full py-16 px-4">
      <form id="userActivationForm" class="flex flex-col items-center justify-center">
      <div class="bg-white shadow rounded lg:w-1/3  md:w-1/2 w-full p-10 mt-16">
        <p tabindex="0" class="focus:outline-none text-2xl font-extrabold leading-6 text-gray-800">User Activation</p>
        <div class="w-full flex items-center justify-between py-5">
          <hr class="w-full bg-gray-400">
          <hr class="w-full bg-gray-400  ">
        </div>
        <div>
          <label id="email" class="text-sm font-medium leading-none text-gray-800">
            Enter User ID
          </label>
          <input aria-labelledby="text" type="text" id="empID" name="empID" class="bg-gray-200 border rounded  text-xs font-medium leading-none text-gray-800 py-3 w-full pl-3 mt-2" required/>
        </div>
        <div class="mt-6  w-full">
          <label for="pass" class="text-sm font-medium leading-none text-gray-800">
            Enter Password
          </label>
          <div class="relative flex items-center justify-center">
            <input id="password" name="password" type="password" class="bg-gray-200 border rounded  text-xs font-medium leading-none text-gray-800 py-3 w-full pl-3 mt-2" required/>
          </div>
        </div>
        <div id="error"></div>
        <div class="mt-8">
          <button role="button" Onclick="validate()" 
            class="focus:ring-2 focus:ring-offset-2 focus:ring-indigo-700 text-sm font-semibold leading-none 
            text-white focus:outline-none bg-indigo-700 border rounded hover:bg-indigo-600 py-4 w-full">ACTIVATE</button>
        </div>
      </div>
    </form>
  </div>

    <!-- <h1>User Activation</h1>
    <form id="userActivationForm">
      <p>Enter User ID:</p>
      <input type="number" id="empID" name="empID" value="" required />
      <p>Enter Password:</p>
      <input type="password" id="password" name="password" value="" required />
      <p id="error"></p>
      <button type="button" onclick="validate()">Activate</button>
    </form>-->

    <script>
      function validate() {
        var userActivationForm = document.getElementById('userActivationForm');
        if (!userActivationForm.checkValidity()) {
          if (userActivationForm.reportValidity) {
            userActivationForm.reportValidity();
          } else {
            alert(msg.ieErrorForm);
          }
        } else {
          activateUser();
        }
      }

      async function activateUser() {
        const userID = document.getElementById('empID').value;
        const password = document.getElementById('password').value;

        const response = await fetch('/approveUser', {
          method: 'POST',
          headers: {
            'Content-type': 'application/json',
          },
          body: JSON.stringify({
            userID,
            password,
          }),
        });

        const data = await response.json();
        console.log('response: ', data);

        const errorMsg = document.getElementById('error');
        if (data.status == 'success') {
          document.getElementById('successfulActivationMessage').innerHTML =
            'Account activated successfully';
          document.getElementById(
            'successfulActivationDivision'
          ).style.visibility = 'visible';
        } else {
          if (data.reason == 'invalid credentials') {
            errorMsg.innerHTML = 'Invalid credentials';

            setTimeout(() => {
              errorMsg.innerHTML = '';
            }, 3000);
          } else {
            errorMsg.innerHTML = 'Error activating. Try Again!';

            setTimeout(() => {
              errorMsg.innerHTML = '';
            }, 3000);
          }
        }
      }
    </script>
  </body>
</html>
