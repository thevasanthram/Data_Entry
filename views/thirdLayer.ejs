<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./thirdLayer.css" />
    <title>Data Entry System</title>
  </head>
  <body>
    <div>
      <div class="nav-bar">
        <div class="user-mode">
          <p id="user-mode-name"><%= username %></p>
        </div>
        <div class="app-name-div">
          <h2 id="app-name">Data Entry System</h2>
          <p class="selected-message">
            Body Number : <%= enteredBodyNumber %> | Category : <%=
            selectedCategory %> | Sub-Category : <%= selectedSubCategory %>
          </p>
        </div>
      </div>
      <div id="title-name-content">
        <p id="title-name">Select Sub-Category :</p>
      </div>
      <div class="main-content">
        <% for(let i = 0 ; i < ShortlistedSubCategoryOptions.length ; i++){ %>
        <form action="/handlesave" method="post">
          <input
            type="hidden"
            name="ShortlistedSubCategoryOption"
            value="<%= ShortlistedSubCategoryOptions[i] %>"
          />
          <button type="submit"><%= ShortlistedSubCategoryOptions[i]%></button>
        </form>
        <%}%>
      </div>
    </div>
    <!-- <form action="/secondlayer" method="get">
      <button type="submit">Go Back</button>
    </form> -->
  </body>
</html>
